# Port exposed on the host (standalone mode)
PORT=3000

# Traefik mode — set your domain here
DOMAIN=example.com
SUBDOMAIN=playground        # results in playground.example.com
PROXY_NETWORK=proxy         # external Docker network Traefik is attached to
